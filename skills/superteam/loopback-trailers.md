# Loopback trailers

Deprecated. Superteam no longer uses `Loopback:` commit trailers for workflow
state. Feedback is classified at intake and routed from visible artifacts, PR
state, review state, and operator prompts.

Do not add new workflow rules to this file. Current routing guidance lives in
`SKILL.md`, `pre-flight.md`, and `routing-table.md`.
